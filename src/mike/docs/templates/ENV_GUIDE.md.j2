{# src/architectai/docs/templates/ENV_GUIDE.md.j2 #}
# Environment Guide

## Configuration Files

{% if config_files %}
The following configuration files were detected:

{% for config in config_files %}
### {{ config.name }}

- **Path**: `{{ config.path }}`
- **Type**: {{ config.type }}

{% if config.variables %}
**Environment Variables**:
{% for var in config.variables %}
- `{{ var.name }}`{% if var.default %} (default: `{{ var.default }}`){% endif %}
{% endfor %}
{% endif %}

{% endfor %}
{% else %}
No standard configuration files detected (e.g., .env, config.yaml, etc.).
{% endif %}

## Environment Variables

{% if env_variables %}
The following environment variables are referenced in the codebase:

| Variable | Files | Context |
|----------|-------|---------|
{% for var in env_variables %}
| `{{ var.name }}` | {{ var.files | join(", ") }} | {{ var.context }} |
{% endfor %}

{% else %}
No environment variables detected in the codebase.
{% endif %}

## Setup Instructions

### Local Development

```bash
# Copy example environment file if exists
cp .env.example .env 2>/dev/null || echo "No .env.example found"

# Edit configuration
# vim .env
```

### Production Deployment

Document production-specific environment setup here.

---

*Generated by ArchitectAI Documentation Agent*